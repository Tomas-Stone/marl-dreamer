# Wrap JaxMARL MPE env to be compatible with our codebase
# Here we should initialize the env, set seeds, return obs, action spaces, etc.
# We'll use the env_mpe.yaml config file to set env parameters

import jax
